<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
  </PropertyGroup>

  <ItemGroup>
    <!-- ASP.NET Core for middleware, filters, HttpContext (includes all Microsoft.Extensions.* transitively) -->
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Pervaxis.Genesis.Base\Pervaxis.Genesis.Base.csproj" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Pervaxis.Genesis.Idempotency.Tests" />
    <InternalsVisibleTo Include="Pervaxis.Genesis.Idempotency.AWS" />
    <InternalsVisibleTo Include="Pervaxis.Genesis.Idempotency.AWS.Tests" />
  </ItemGroup>

</Project>
